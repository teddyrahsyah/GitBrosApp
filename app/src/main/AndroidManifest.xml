<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.develo.gitbros">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="id.develo.gitbros.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="id.develo.gitbros.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitBros">
        <provider
            android:name=".GitBrosProvider"
            android:authorities="id.develo.gitbros"
            android:enabled="true"
            android:exported="true"
            android:readPermission="id.develo.gitbros.READ_DATABASE"
            android:writePermission="id.develo.gitbros.WRITE_DATABASE"/>

        <activity android:name=".FavoriteActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask" />

        <receiver android:name=".reminder.AlarmReceiver" />
    </application>

</manifest>